# 排除内嵌 Python 环境（太大了，别人应该自己配置或从 Release 下载）
python_embed/

# 排除临时文件
temp_score.png
temp_score.musicxml
temp_score.mid
temp_score.xml
homr_musicxml_gui_settings.json
*.zip

# 排除 Python 缓存
__pycache__/
*.py[cod]

# 排除调试输出目录
output/
_debug/

# 排除 AI 模型文件（让代码自动下载，保持仓库轻量）
*.onnx

# 排除压缩包和历史记录
*.rar
History/
res/   # 如果 res 文件夹里只有你截图里的那些 UI 图片则保留，如果有大文件就排除

# 排除运行产生的临时文件
*.png
*.musicxml
*.mid
*.autosave
